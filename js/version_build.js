// Réécrit AUTOMATIQUEMENT à chaque déploiement (voir .github/workflows/pages.yml).
// Ne pas éditer à la main. 'dev' en local => la porte de version ne bloque jamais
// tant que le jeu n'est pas déployé. BUILD_VERSION = numéro lisible (0.1, 0.2…),
// incrémenté à chaque déploiement ; affiché sur l'écran titre.
export const BUILD_SHA = 'dev';
export const BUILD_VERSION = '0.1';
